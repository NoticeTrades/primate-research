# ───────────────────────────────────────
# Neon Postgres Database
# ───────────────────────────────────────
# 1. Go to https://neon.tech and sign up (free)
# 2. Create a project → copy the connection string
# 3. Paste it here AND add it to Vercel environment variables
DATABASE_URL=postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# ───────────────────────────────────────
# Resend — Email Notifications
# ───────────────────────────────────────
# 1. Go to https://resend.com and sign up (free)
# 2. Create an API key → paste it here
RESEND_API_KEY=re_xxxxxxxxx

# The "from" address for notification emails.
# Free tier: use "Primate Research <notifications@resend.dev>"
# Custom domain: verify in Resend, then use your own address.
RESEND_FROM_EMAIL="Primate Research <notifications@resend.dev>"

# Optional: get a copy of every report notification at this address (e.g. your personal email).
# Useful if the email you signed up with doesn't receive the blast or you want a backup.
# NOTIFY_CC_EMAIL=your-email@gmail.com

# ───────────────────────────────────────
# Admin / Notify Secret
# ───────────────────────────────────────
# Pick any secret passphrase. Used to access /admin and send notifications.
NOTIFY_SECRET=your-secret-key-here

# ───────────────────────────────────────
# Alpha Vantage — backup for /dashboard/valuation when Yahoo Finance blocks the server (recommended on Vercel)
# Free key: https://www.alphavantage.co/support/#api-key — same variable used by MOST / market-data fallbacks.
# ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key

# Financial Modeling Prep — optional for /dashboard/valuation (quarterly history + period tables)
# Valuation page works without this key using Yahoo + optional Alpha Vantage ETF snapshots.
# Use stable endpoints only (legacy /api/v3 is deprecated for new accounts after Aug 2025).
# HTTP 402 = plan/credits; HTTP 401 = invalid key. Docs: https://site.financialmodelingprep.com/developer/docs/stable
# Use the exact name FMP_API_KEY (also accepts FINANCIAL_MODELING_PREP_API_KEY, FMP_KEY).
# In Vercel: Settings → Environment Variables → add for Production + Preview; redeploy.
# FMP_API_KEY=your-fmp-key

# ───────────────────────────────────────
# FRED (St. Louis Fed) — CPI / macro series on /dashboard/inflation
# ───────────────────────────────────────
# Free key: https://fred.stlouisfed.org/docs/api/api_key.html
# If set, /api/cpi uses the official JSON API; otherwise the same data loads via public CSV.
# FRED_API_KEY=your-fred-api-key

# ───────────────────────────────────────
# Site URL (used in email links)
# ───────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://primate-research.vercel.app

# ───────────────────────────────────────
# Twilio — SMS for live trade alerts (optional)
# ───────────────────────────────────────
# 1. Sign up at https://twilio.com and get a phone number
# 2. Account SID and Auth Token from Twilio console
# 3. Use the Twilio number in E.164 format (e.g. +15551234567)
# If not set, SMS alerts are skipped; email and bell still work.
# TWILIO_ACCOUNT_SID=ACxxxxxxxx
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+15551234567

# ───────────────────────────────────────
# Twelve Data — Real-time futures (NQ, ES, YM, RTY, CL) for ticker & Live Trades
# ───────────────────────────────────────
# Without this, the site uses Yahoo Finance which is 10–15 min delayed for CME futures.
# Get a key at https://twelvedata.com — free tier has real-time. Add to Vercel env vars.
# TWELVE_DATA_API_KEY=your-twelve-data-key

# ───────────────────────────────────────
# Alpha Vantage — MOST command + nav bar market data (futures, DXY, GC, SI)
# ───────────────────────────────────────
# 1. Go to https://www.alphavantage.co/support/#api-key
# 2. Get a free API key and add it here and in Vercel env vars
# Used for: MOST terminal (gainers/losers), breaking news, AND nav bar quotes when Twelve Data is out or rate-limited.
# Free tier: 5 requests/min — results are cached 90s per symbol to stay under limit.
# ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key

# ───────────────────────────────────────
# X (Twitter) — Breaking news from @spectatorindex (optional)
# ───────────────────────────────────────
# 1. Go to https://developer.x.com — create a project and app
# 2. In the app, generate a Bearer Token (read-only)
# 3. Add it here (or as TWITTER_BEARER_TOKEN) and in Vercel env vars
# If set, the breaking banner shows the latest tweet from @spectatorindex first.
# X_BEARER_TOKEN=your-x-bearer-token

# ───────────────────────────────────────
# Finnhub — Market news for breaking banner (optional)
# ───────────────────────────────────────
# Free API key at https://finnhub.io — used for general market news in the breaking-news ticker.
# FINNHUB_API_KEY=your-finnhub-token

# ───────────────────────────────────────
# YouTube (optional — view counts + live indicator on homepage)
# ───────────────────────────────────────
# YOUTUBE_API_KEY=your-youtube-data-api-v3-key
# Live indicator uses channel handle (no @) or channel ID:
# YOUTUBE_CHANNEL_HANDLE=noticetrades
# YOUTUBE_CHANNEL_ID=UCxxxxxxxx  # optional; overrides handle if set
